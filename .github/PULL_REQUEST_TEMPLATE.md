## What & why

<!-- What does this change, and why? Link any related issue. -->

## Checklist

- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] No secrets committed (keys stay in env / Supabase, never in the repo)
- [ ] Updated `.env.local.example` and the README if configuration changed
